{
  "name": "sih26100-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "SIH26100 — AI-Powered Integrated Bid Compliance Verification & Seller Platform Monorepo",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ai\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && mvn spring-boot:run",
    "dev:ai": "cd ai-service && py -3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "cd backend && mvn clean compile",
    "test": "npm run test:frontend && npm run test:backend && npm run test:ai",
    "test:frontend": "npm --prefix frontend run build",
    "test:backend": "cd backend && mvn test-compile",
    "test:ai": "cd ai-service && py -3 -m pytest tests/",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:build": "docker compose build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
